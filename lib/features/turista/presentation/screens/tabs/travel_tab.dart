import 'package:flutter/material.dart';

class TravelTab extends StatelessWidget {
  const TravelTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Travel Tab')),
    );
  }
}
