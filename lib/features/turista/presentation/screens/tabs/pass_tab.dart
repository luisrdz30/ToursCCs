import 'package:flutter/material.dart';

class PassTab extends StatelessWidget {
  const PassTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Pass Tab')),
    );
  }
}
